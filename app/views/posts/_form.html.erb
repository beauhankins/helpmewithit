<%= form_for(@post) do |f| %>
  <% if @post.errors.any? %>
    <div id="error_explanation" class="p2 border mb3">
      <span class="h2 bold">Please fix the following <%= pluralize(@post.errors.count, "error") %>:</span>

      <ul class="mt2 mb0">
      <% @post.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="mb2">
    <%= f.label :title, class: 'bold' %>
    <%= f.text_field :title,  class: 'field col-12 mt1', :placeholder => "Headline for your ad"  %>
  </div>
  <div class="mb2">
    <%= f.label :contact, class: 'bold' %>
    <%= f.text_field :contact, class: 'field col-12 mt1', :placeholder => "Phone and/or email address"  %>
  </div>
  <div class="mb2">
    <%= f.label "Task Already Done?" %> <br />
    <%= f.check_box :task_done %> <br />
</div>
  <div class="mb2">
    <%= f.label :category, class: 'bold' %>
    <%= f.select :category, Post::POST_TYPES,{},class: 'field col-12 mt1'  %>
  </div>
  <div class="mb2">
    <%= f.label :content, class: 'bold' %>
    <div class="mt1">
      <%= f.text_area :content , class: 'tinymce col-12 field', rows: 10, :placeholder => "Your main content goes here. Please include details such as your location(Suva,Nadi,Lautoka,SavuSavu etc)"%>
    </div>

  </div>

  <span class="block h4 mb1 bold">Upload a picture</span>
  <%= f.file_field :postpic %>

  <div class="mt4 right-align">
    <%= f.submit "Post task", class: "btn btn-primary bg-green"%>
  </div>

<% end %>
<%= tinymce %>


